#ifndef GAME_H
#define GAME_H

#include <iostream>
#include <string>

    void runGame();
    void playerIn(); //only takes in forward, climb or descent or attack as input.
    void arrayPositions();
#endif
